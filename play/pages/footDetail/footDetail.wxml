<view>
  <view class='footDetail'>
    <text class='footDetail-clear' bindtap='clearFootmark' wx:if="{{footList.length > 0}}">清空</text>
  </view>
  <publish-item list="{{footList}}" imageType="footmark"></publish-item>
</view>
